services:
  web:
    build: .
    command: python manage.py runserver 0.0.0.0:8000
    ports:
      - "8000:8000"
    volumes:
      - .:/app

    environment:
      #-- DEV STAGE --
      #- DEBUG=1
      - DEBUG=0
      - SECRET_KEY=supersecreto
      - ALLOWED_HOSTS=*
